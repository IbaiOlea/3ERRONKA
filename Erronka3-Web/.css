/* Reset de márgenes y padding */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Asegurar que el HTML y el BODY ocupen toda la altura */
html, body {
    height: 100%;
}

/* Configuración del cuerpo */
body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    text-align: center;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

/* Encabezado */
header {
    background-color: #4CAF50;
    color: white;
    padding: 20px;
    font-size: 24px;
    font-weight: bold;
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    width: 100%;
}

/* Botón de logout */
.logout-button {
    background-color: darkred;
    color: white;
    border: none;
    padding: 10px 15px;
    font-size: 14px;
    cursor: pointer;
    border-radius: 5px;
    position: absolute;
    right: 20px;
}

.logout-button:hover {
    background-color: red;
}

/* Contenido principal */
main {
    flex-grow: 1; /* Permite que el main ocupe el espacio disponible y empuje el footer abajo */
    padding: 20px;
    background-color: white;
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Contenedor de información */
.info {
    margin: 20px auto;
    padding: 20px;
    background: #ddd;
    width: 50%;
    border-radius: 10px;
}

/* Estilo del login */
.login-container {
    background: white;
    padding: 40px;
    border-radius: 10px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    width: 400px;
}

.login-container h2 {
    font-size: 28px;
    margin-bottom: 20px;
}

.login-container input {
    width: 100%;
    padding: 15px;
    font-size: 18px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

.login-container button {
    width: 100%;
    padding: 15px;
    font-size: 18px;
    border: none;
    background-color: #4CAF50;
    color: white;
    cursor: pointer;
    border-radius: 5px;
    margin-top: 10px;
}

.login-container button:hover {
    background-color: #45a049;
}

/* Footer fijo abajo SIEMPRE */
footer {
    background-color: #4CAF50;
    color: white;
    padding: 10px;
    font-size: 14px;
    width: 100%;
    text-align: center;
    margin-top: auto;
}

/* Sección de productos */
.productos {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
    margin-top: 20px;
}